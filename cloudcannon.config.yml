# cloudcannon.config.yml
collections:
  pages:
    path: "*.html"
    parse_mode: html
    output: true
    editor:
      show: true
      preview_path: "index.html"
      _options:
        body:
          widgets:
            - type: "html"
