collections:
  pages:
    path: "*.html"
    parse_mode: html
    output: true
    _inputs:
      title:
        label: "Page Title"
        type: "text"
        comment: "The main title of the page"
      content:
        label: "Page Content"
        type: "html"
        comment: "The main body of the page"
