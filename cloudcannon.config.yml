# cloudcannon.config.yml

# Tell CloudCannon where to find your static files (like images)
_paths:
  uploads: /images/

# Configure the editor to find your pages in the root
_editor:
  page_title: page
  paths:
    - "*.html"
